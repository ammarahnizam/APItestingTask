import { ValidationErrorWithStatus } from './';
export declare class ValidationErrorWithStatusSilent extends ValidationErrorWithStatus {
    constructor(status: number, message: string, errorLocale: string, payload?: any);
}
