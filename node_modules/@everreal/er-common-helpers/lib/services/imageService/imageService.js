"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var urlJoin = require("url-join");
var validators = require("../../helpers/validation");
exports.isBlob = validators.isBlob;
exports.isImageBlob = function (image) { return exports.isBlob(image) && image.type.includes('image'); };
exports.getImagePath = function (_a) {
    var _b = _a === void 0 ? {} : _a, _c = _b.image, image = _c === void 0 ? '' : _c, _d = _b.basePath, basePath = _d === void 0 ? '' : _d;
    var startPath = basePath;
    if (!image) {
        return image;
    }
    if (exports.isImageBlob(image)) {
        return image.preview;
    }
    if (typeof image === 'string') {
        if (image.startsWith('http://') || image.startsWith('https://') || image.startsWith('//')) {
            return image;
        }
        return urlJoin(startPath, image);
    }
    if (typeof image === 'object' && !!image.resourcePath) {
        return urlJoin(startPath, image.resourcePath);
    }
    return image;
};
//# sourceMappingURL=imageService.js.map