import { MATH_OPERATORS, COMPARISON_SIGNS } from './constants';
export declare const calculateWithSign: (val1: number, val2: number, sign: MATH_OPERATORS) => number;
export declare const compareWithSign: (value: any, search: any, sign: COMPARISON_SIGNS) => boolean;
