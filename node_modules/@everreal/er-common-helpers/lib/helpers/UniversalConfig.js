"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var undefsafe = require("undefsafe");
var UniversalConfig = /** @class */ (function () {
    function UniversalConfig(config) {
        if (config === void 0) { config = {}; }
        this.config = null;
        this.config = config;
    }
    UniversalConfig.getConfig = function (path) {
        if (path === void 0) { path = ''; }
        if (!UniversalConfig.getCurrentInstance()) {
            return null;
        }
        var config = UniversalConfig.getCurrentInstance().config;
        if (path) {
            return undefsafe(config, path);
        }
        return config;
    };
    UniversalConfig.getCurrentInstance = function () {
        return UniversalConfig.currentInstance;
    };
    UniversalConfig.setCurrentInstance = function (config) {
        if (config === void 0) { config = {}; }
        UniversalConfig.currentInstance = new UniversalConfig(config);
    };
    return UniversalConfig;
}());
exports.UniversalConfig = UniversalConfig;
//# sourceMappingURL=UniversalConfig.js.map