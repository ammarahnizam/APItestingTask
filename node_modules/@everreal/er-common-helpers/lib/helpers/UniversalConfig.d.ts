export declare class UniversalConfig {
    static getConfig(path?: string): any;
    static getCurrentInstance(): UniversalConfig;
    static setCurrentInstance(config?: any): void;
    private static currentInstance;
    private config;
    constructor(config?: any);
}
