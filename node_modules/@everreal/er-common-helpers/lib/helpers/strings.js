"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var validation_1 = require("./validation");
exports.capitaliseString = function (str) {
    if (str === void 0) { str = ''; }
    if (validation_1.isEmpty(str)) {
        return '';
    }
    var value = str.toString();
    var firstLetter = value[0] || '';
    var restOfString = value.slice(1) || '';
    return "" + firstLetter.toUpperCase() + restOfString;
};
exports.extractFirstTextFoundBetween = function (str, before, after) {
    if (!str || !before || !after) {
        return null;
    }
    if (typeof str !== 'string' || typeof before !== 'string' || typeof after !== 'string') {
        return null;
    }
    if (before === after) {
        return null;
    }
    var beforeLength = before.length + 1;
    var indexOfBefore = str.indexOf(before);
    if (indexOfBefore === -1) {
        return null;
    }
    var relativeIndexOfAfter = str
        .slice(indexOfBefore)
        .slice(beforeLength)
        .indexOf(after);
    if (relativeIndexOfAfter === -1) {
        return null;
    }
    var indexOfAfter = indexOfBefore + beforeLength + relativeIndexOfAfter;
    if (indexOfAfter <= indexOfBefore) {
        return null;
    }
    return str.slice(indexOfBefore + before.length, indexOfAfter);
};
function getFullName(data) {
    if (!data) {
        return '';
    }
    var fullName = '';
    if (data.firstName && typeof data.firstName === 'string') {
        fullName += data.firstName.trim();
    }
    if (data.lastName && typeof data.lastName === 'string') {
        fullName += " " + data.lastName.trim();
    }
    return fullName;
}
exports.getFullName = getFullName;
function getFullNameOrEmail(data) {
    var fullName = getFullName(data);
    if (!fullName && !!data.email) {
        return data.email;
    }
    return fullName;
}
exports.getFullNameOrEmail = getFullNameOrEmail;
//# sourceMappingURL=strings.js.map