"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var clientLogger = require("../helpers/clientLogger");
exports.loggerMiddleware = function () { return function (store) { return function (next) { return function (action) {
    var dispatch = store.dispatch, getState = store.getState;
    if (typeof action === 'function') {
        return action(dispatch, getState);
    }
    try {
        clientLogger.addBreadcrumb('redux', action.type);
        return next(action);
    }
    catch (err) {
        clientLogger.error('[redux-logger-middleware] Reporting error to Sentry:', err);
        clientLogger.captureException(err, {
            action: action,
            state: store.getState(),
        });
    }
}; }; }; };
//# sourceMappingURL=loggerMiddleware.js.map